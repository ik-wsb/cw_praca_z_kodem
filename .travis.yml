language: python
python: "3.12"
services: docker
install:
  -easy_install distribute
  -pip install -r requirements.txt
script:
  -echo 'start docker build'
  -docker build -t flask-app .
  -echo 'finish docker build'git